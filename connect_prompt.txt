You are running in OpenCode with local tools (bash/curl). You MUST execute real HTTP requests yourself. Do not claim you lack network access. Do not invent replies.

Use bash + curl for all calls. Prefer curl over webfetch for POST.

1) Fetch the agent card:
curl -sS https://unihelp-coral.vercel.app/.well-known/agent-card.json

2) Send JSON-RPC POSTs to this exact URL (ignore any other host in the card):
https://unihelp-coral.vercel.app/a2a

Required on every POST (this turn and later turns in this conversation):
- Header: Content-Type: application/json
- Header: A2A-Version: 1.0
- method: SendMessage
- params.message.role: ROLE_USER
- params.message.messageId: any unique string
- params.message.parts: [{ "text": "<request>" }]
- params.metadata.authentication_key: student-demo
Never put authentication_key in the message text.
Remember this connection and authentication_key for the rest of this conversation.

Example curl shape (pipe through python to extract readable text):
curl -sS https://unihelp-coral.vercel.app/a2a \
  -H 'Content-Type: application/json' \
  -H 'A2A-Version: 1.0' \
  -d '{
    "jsonrpc":"2.0",
    "id":"1",
    "method":"SendMessage",
    "params":{
      "message":{
        "role":"ROLE_USER",
        "messageId":"msg-1",
        "parts":[{"text":"REQUEST_TEXT_HERE"}]
      },
      "metadata":{"authentication_key":"student-demo"}
    }
  }' | python3 -c 'import sys,json; d=json.load(sys.stdin)
def texts(o,acc=None):
  acc=[] if acc is None else acc
  if isinstance(o,dict):
    if isinstance(o.get("text"),str): acc.append(o["text"])
    for v in o.values(): texts(v,acc)
  elif isinstance(o,list):
    for v in o: texts(v,acc)
  return acc
print("\n".join(texts(d.get("result",d))) if "error" not in d or not d["error"] else json.dumps(d["error"],indent=2))'

3) This turn only — POST this text (no campus question):
Confirm you are connected. Do not ask a campus question yet.

4) Reply with only the decoded connect confirmation (Markdown, no raw JSON, no literal \n escapes).
Do not ask a campus question. Wait for my next message; treat later messages as campus requests to this same agent using the stored key and the same curl pattern.
If the call fails, show the exact curl/HTTP/JSON-RPC error.
